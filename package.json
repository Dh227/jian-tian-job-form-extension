{
  "name": "jian-tian-job-form-extension",
  "version": "0.2.0",
  "private": true,
  "description": "本地优先的简历识别、岗位档案与网申资料助手",
  "scripts": {
    "check": "node --check background.js && node --check core.js && node --check resume-core.js && node --check job-core.js && node --check file-parser.js && node --check storage.js && node --check content.js && node --check popup.js && node --check options.js",
    "test": "node --test tests/*.test.cjs"
  }
}
