# 隐私说明

“简填 · 网申资料助手”是一个本地优先的浏览器扩展，不包含远程服务器、AI 接口、统计 SDK 或广告代码。

## 本机保存的数据

- 基本资料和工作区设置保存在 `chrome.storage.local`。
- 母版原始文件、提取文本、结构化简历、岗位档案和截图保存在扩展自己的 IndexedDB。
- 数据仅存在于当前浏览器配置中，不进行云同步。
- 用户可以单独删除母版或岗位，也可以使用“清除全部本机资料”删除全部数据。

## 页面访问

- 只有用户点击扩展时，扩展才通过 `activeTab` 临时访问当前网页。
- “保存当前岗位”会读取当前页面的岗位结构化数据、标题、招聘正文和网址，并截取当前可见区域。
- 提取结果会先进入可编辑的岗位档案；扩展不会自动访问其他标签页或后台遍历招聘网站。

## 简历与提示词

- PDF 和 DOCX 由扩展内置的本地解析库处理，不会上传。
- 扩展不对扫描版 PDF 或岗位截图执行 OCR。
- “复制提示词”只在用户点击后将生成内容写入系统剪贴板；扩展不会读取剪贴板。
- 用户自行决定是否把提示词、母版或截图发送给 Codex 或其他服务，并应遵守相应服务的隐私政策。

## 不处理的内容

- 扩展不会读取或保存密码、验证码、银行卡、社保、公积金等信息。
- 扩展不会自动点击提交、投递、登录或法律声明，也不会绕过网站限制。
- 隐私授权、背景调查、竞业限制和真实性承诺始终由用户人工处理。

卸载扩展后，浏览器会按照自身规则删除该扩展的本地存储数据。
